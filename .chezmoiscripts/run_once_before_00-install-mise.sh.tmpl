#!/bin/bash
# Install Mise tool manager if not already present.
# Code generated by chezmoi. DO NOT EDIT.

{{ template "script_utils.sh.tmpl" . }}

# Ensure local bin is in PATH so we can check if it is already installed
export PATH="${HOME}/.local/bin:${PATH}"

if command -v mise >/dev/null 2>&1 || [ -f "${HOME}/.local/bin/mise" ]; then
  success "Mise is already installed, skipping installation."
  exit 0
fi

info "Installing mise..."
mkdir -p "${HOME}/.local/bin"
curl -fsSL https://mise.run | sh
