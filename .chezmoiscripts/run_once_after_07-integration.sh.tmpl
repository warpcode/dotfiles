#!/bin/bash
{{ template "install_packages.tmpl" (dict "group" "integration" "context" .) }}

if command -v flatpak >/dev/null 2>&1; then
  flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo || true
fi
