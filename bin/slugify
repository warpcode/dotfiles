#!/usr/bin/env bash

local input=''

 if [[ -p /dev/stdin ]]; then
    input="$(cat -)"
  else
    input="${*}"
  fi

echo "$input" | sed -e 's/[^[:alnum:]]/-/g' | tr -s '-' | sed 's/\(^-*\|-*$\)//g' | tr A-Z a-z
