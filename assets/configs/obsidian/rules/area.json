{
  "type": "area",
  "folder": "Areas",
  "validation": {
    "require_all": ["title"]
  },
  "fields": {
    "context": { "type": "select", "values": ["work", "personal"] },
    "description": { "from_type": "default", "from_field": "description" }
  }
}
