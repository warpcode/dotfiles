{
  "commands": {
    "rules": [
      {
        "pattern": "(?:^|[;&|\\s])(?:/usr(?:/local)?/bin/)?python[0-9.]*\\s+-c\\b",
        "match": "regex",
        "perm": "deny",
        "reason": "Python inline execution is unsafe."
      },
      {
        "pattern": "(?:^|[;&|\\s])(?:/(?:usr/)?(?:local/)?bin/)?(?:node\\s+-e|perl\\s+-e|ruby\\s+-e|php\\s+-r)\\b",
        "match": "regex",
        "perm": "deny",
        "reason": "Inline script evaluation is unsafe."
      },
      {
        "pattern": "\\brm\\s+.*-[a-zA-Z]*(?:r.*f|f.*r).*\\s+[/~]",
        "match": "regex",
        "perm": "deny",
        "reason": "Recursive force removal targeting root or home directory is strictly forbidden."
      },
      {
        "pattern": "rm -rf /",
        "match": "prefix",
        "perm": "deny",
        "reason": "Root bulk deletion forbidden."
      },
      {
        "pattern": "rm -rf ~",
        "match": "prefix",
        "perm": "deny",
        "reason": "Home bulk deletion forbidden."
      },
      {
        "pattern": "rm -rf $HOME",
        "match": "prefix",
        "perm": "deny",
        "reason": "Home bulk deletion forbidden."
      },
      {
        "pattern": "mkfs*",
        "match": "glob",
        "perm": "deny",
        "reason": "Disk formatting commands are forbidden."
      },
      {
        "pattern": "dd if=*",
        "match": "glob",
        "perm": "deny",
        "reason": "Low-level block device overwriting via dd is forbidden."
      },
      {
        "pattern": "chmod -R 777*",
        "match": "glob",
        "perm": "deny",
        "reason": "Global permissive permissions (777) are forbidden."
      },
      {
        "pattern": "shutdown*",
        "match": "glob",
        "perm": "deny",
        "reason": "System shutdown commands are forbidden."
      },
      {
        "pattern": "reboot*",
        "match": "glob",
        "perm": "deny",
        "reason": "System reboot commands are forbidden."
      },
      {
        "pattern": "poweroff*",
        "match": "glob",
        "perm": "deny",
        "reason": "System poweroff commands are forbidden."
      },
      {
        "pattern": "(?i)\\b(DROP\\s+DATABASE|TRUNCATE\\s+TABLE|FLUSHALL|FLUSHDB)\\b",
        "match": "regex",
        "perm": "deny",
        "reason": "Destructive database operations (DROP/TRUNCATE/FLUSHALL) are strictly forbidden."
      },
      {
        "pattern": "(?i)\\b(AWS_SECRET_ACCESS_KEY|OPENAI_API_KEY|ANTHROPIC_API_KEY|GITHUB_TOKEN)\\s*=\\s*['\"]?[a-zA-Z0-9_\\-]{12,}",
        "match": "regex",
        "perm": "deny",
        "reason": "Passing plaintext API credentials via inline terminal environment variables is forbidden."
      },
      {
        "pattern": ".*\\|\\s*(?:/bin/)?(?:sudo\\s+)?(?:bash|sh|zsh).*",
        "match": "regex",
        "perm": "deny",
        "reason": "Piping commands directly into shell interpreters is forbidden."
      },
      {
        "pattern": "\\b(?:curl|wget)\\b.*\\|\\s*(?:sudo\\s+)?(?:/usr(?:/local)?/bin/|/bin/)?(?:ba|z)?sh\\b",
        "match": "regex",
        "perm": "deny",
        "reason": "Piping remote network scripts directly into a shell interpreter is forbidden."
      },
      {
        "pattern": "\\b(?:curl|wget)\\b.*\\|\\s*(?:sudo\\s+)?(?:/usr(?:/local)?/bin/|/bin/)?python[0-9.]*\\b",
        "match": "regex",
        "perm": "deny",
        "reason": "Piping remote network scripts directly into Python is forbidden."
      },
      {
        "pattern": "\\bfind\\b.*-(?:exec|execdir|ok|okdir|delete|fprint|fprintf|fls)\\b",
        "match": "regex",
        "perm": "deny",
        "reason": "Destructive or modifying find operations (-delete, -exec, -ok, -fprint, etc.) are strictly forbidden."
      },
      {
        "pattern": "(?:^|[;&|]\\s*)(?:env(?:\\s+-[a-zA-Z0-9_-]+|\\s+[a-zA-Z0-9_]+=[^\\s]*)*\\s*(?:$|[;&|])|printenv|export(\\s+-p)?|declare\\s+-p|typeset\\s+-p)\\b",
        "match": "regex",
        "perm": "deny",
        "reason": "Environment inspection commands (env/printenv/export/declare/typeset) are blocked to prevent exposing injected secrets."
      },
      {
        "pattern": "(?:^|[;&|]\\s*)set\\s*(?:$|[;&|])",
        "match": "regex",
        "perm": "deny",
        "reason": "Bare set command is blocked: it would expose injected secrets to model-visible output."
      },
      {
        "pattern": "\\bgit\\s+push\\b.*(?:\\s|^)(?:-f\\b|--force\\b|--force-with-lease\\b)",
        "match": "regex",
        "perm": "deny",
        "reason": "Force pushing via git push is forbidden. Use regular branch pushes and open a PR."
      },
      {
        "pattern": "(^|[;&|]\\s*)(litellm\\b.*)$",
        "match": "regex",
        "perm": "replace",
        "replace": "\\1cloakenv run -- \\2",
        "reason": "Prepend cloakenv run for litellm commands"
      }
    ]
  },
  "files": {
    "rules": [
      {
        "pattern": "(?i)(?:^|[\\/\\\\])(?:.*\\.env(?:\\..*)?|.*env\\.(?:tmpl|template|vault).*|.*dot_env.*)$",
        "match": "regex",
        "perm": "deny",
        "reason": "Direct access to environment and template files (.env, env.tmpl, etc.) is blocked. Use 'df.config resolve' or environment variables."
      },
      {
        "pattern": "\\.(?:pem|key|p12|pfx|keystore|jks|kdbx)$",
        "match": "regex",
        "perm": "deny",
        "reason": "Cryptographic keys, certificates, keystores, and vault databases are protected."
      },
      {
        "pattern": "(?:id_(?:rsa|ed25519|ecdsa|dsa).*|.*\\.pub)$",
        "match": "regex",
        "perm": "deny",
        "reason": "SSH private keys and key files are protected."
      },
      {
        "pattern": "(?:credentials|secrets)\\.json",
        "match": "regex",
        "perm": "deny",
        "reason": "Credential and secret JSON files are protected."
      },
      {
        "pattern": "\\.(?:netrc|npmrc)$",
        "match": "regex",
        "perm": "deny",
        "reason": "Authentication and package manager credential files (.netrc, .npmrc) are protected."
      },
      {
        "pattern": "~/.ssh/*",
        "match": "glob",
        "perm": "deny",
        "reason": "Direct access to SSH configurations and keys is forbidden. Use ssh-agent or keychain credentials."
      },
      {
        "pattern": "~/.aws/*",
        "match": "glob",
        "perm": "deny",
        "reason": "AWS credentials and configs are protected."
      },
      {
        "pattern": "~/.gnupg/*",
        "match": "glob",
        "perm": "deny",
        "reason": "GPG keys and configs are protected."
      },
      {
        "pattern": "~/.docker/*",
        "match": "glob",
        "perm": "deny",
        "reason": "Docker configuration and credentials are protected."
      },
      {
        "pattern": "~/.kube/*",
        "match": "glob",
        "perm": "deny",
        "reason": "Kubernetes credentials and configs are protected."
      },
      {
        "pattern": "~/.config/cloakenv/*",
        "match": "glob",
        "perm": "deny",
        "reason": "Cloakenv credentials are protected."
      },
      {
        "pattern": "~/.git-credentials",
        "match": "glob",
        "perm": "deny",
        "reason": "Git credentials file is protected."
      },
      {
        "pattern": "~/.config/gh/hosts.yml",
        "match": "glob",
        "perm": "deny",
        "reason": "GitHub CLI token configuration is protected."
      },
      {
        "pattern": "/etc/shadow*",
        "match": "glob",
        "perm": "deny",
        "reason": "System shadow password file is protected."
      },
      {
        "pattern": "/etc/passwd*",
        "match": "glob",
        "perm": "deny",
        "reason": "System user database is protected."
      }
    ]
  },
  "prompts": {
    "rules": [
      {
        "pattern": "-----BEGIN (?:[A-Z0-9_-]+ )?PRIVATE KEY(?: BLOCK)?-----[\\s\\S]*?(?:-----END (?:[A-Z0-9_-]+ )?PRIVATE KEY(?: BLOCK)?-----|$)",
        "match": "regex",
        "perm": "deny",
        "reason": "Private Key Header detected; prompt submission is blocked."
      },
      {
        "pattern": "sk-(?:proj-|admin-|svcacct-)?[a-zA-Z0-9_-]{20,}",
        "match": "regex",
        "perm": "replace",
        "reason": "OpenAI API Key redacted",
        "replace": "[REDACTED_SECRET_OPENAI_API_KEY]"
      },
      {
        "pattern": "sk-ant-(?:api[0-9]{2}-)?[A-Za-z0-9_-]{30,}",
        "match": "regex",
        "perm": "replace",
        "reason": "Anthropic API Key redacted",
        "replace": "[REDACTED_SECRET_ANTHROPIC_API_KEY]"
      },
      {
        "pattern": "(?:ghp|gho|ghu|ghs|ghr)_[A-Za-z0-9_]{36,}",
        "match": "regex",
        "perm": "replace",
        "reason": "GitHub Token redacted",
        "replace": "[REDACTED_SECRET_GITHUB_TOKEN]"
      },
      {
        "pattern": "github_pat_[A-Za-z0-9_]{80,}",
        "match": "regex",
        "perm": "replace",
        "reason": "GitHub Fine-Grained Token redacted",
        "replace": "[REDACTED_SECRET_GITHUB_PAT]"
      },
      {
        "pattern": "\\b(?:A3T[A-Z0-9]|AKIA|AGPA|AIDA|AROA|AIPA|ANPA|ANVA|ASIA)[A-Z0-9]{16}\\b",
        "match": "regex",
        "perm": "replace",
        "reason": "AWS Access Key redacted",
        "replace": "[REDACTED_SECRET_AWS_ACCESS_KEY]"
      },
      {
        "pattern": "AIza[0-9A-Za-z\\-_]{35}",
        "match": "regex",
        "perm": "replace",
        "reason": "Google AI / GCP Key redacted",
        "replace": "[REDACTED_SECRET_GOOGLE_AI_KEY]"
      },
      {
        "pattern": "xox[baprs]-[0-9]{10,13}-[0-9]{10,13}[a-zA-Z0-9-]*",
        "match": "regex",
        "perm": "replace",
        "reason": "Slack Token redacted",
        "replace": "[REDACTED_SECRET_SLACK_TOKEN]"
      },
      {
        "pattern": "(?i)\\b(password|passwd|secret|api_key|apikey|access_token|auth_token)(\\s*[:=]\\s*)(?:['\"][^'\"]{8,}['\"]|[A-Za-z0-9!@#$%^&*()_+\\-=\\[\\]{};:,.<>?/]{8,})",
        "match": "regex",
        "perm": "replace",
        "reason": "Password/secret assignment redacted",
        "replace": "\\1\\2[REDACTED_SECRET_PASSWORD]"
      },
      {
        "pattern": "(?i)\\b(bearer\\s+)[a-zA-Z0-9_\\-\\.]{25,}",
        "match": "regex",
        "perm": "replace",
        "reason": "Bearer Token redacted",
        "replace": "\\1[REDACTED_BEARER_TOKEN]"
      },
      {
        "pattern": "(?i)\\b(aws_secret_access_key|aws_session_token)(\\s*[:=]\\s*)['\"]?([A-Za-z0-9/+=]{40})['\"]?",
        "match": "regex",
        "perm": "replace",
        "reason": "AWS Secret Key redacted",
        "replace": "\\1\\2[REDACTED_SECRET_AWS_SECRET_KEY]"
      },
      {
        "pattern": "hf_[a-zA-Z0-9]{34,}",
        "match": "regex",
        "perm": "replace",
        "reason": "Hugging Face Token redacted",
        "replace": "[REDACTED_SECRET_HUGGINGFACE_TOKEN]"
      },
      {
        "pattern": "(?:sk|pk)_(?:live|test)_[0-9a-zA-Z]{24,}",
        "match": "regex",
        "perm": "replace",
        "reason": "Stripe API Key redacted",
        "replace": "[REDACTED_SECRET_STRIPE_KEY]"
      }
    ]
  },
  "output": {
    "rules": [
      {
        "pattern": "-----BEGIN (?:[A-Z0-9_-]+ )?PRIVATE KEY(?: BLOCK)?-----[\\s\\S]*?(?:-----END (?:[A-Z0-9_-]+ )?PRIVATE KEY(?: BLOCK)?-----|$)",
        "match": "regex",
        "perm": "replace",
        "replace": "[REDACTED_PRIVATE_KEY]",
        "reason": "Private Key material redacted from tool output"
      },
      {
        "pattern": "sk-(?:proj-|admin-|svcacct-)?[a-zA-Z0-9_-]{20,}",
        "match": "regex",
        "perm": "replace",
        "replace": "[REDACTED_SECRET_OPENAI_API_KEY]",
        "reason": "OpenAI API Key redacted"
      },
      {
        "pattern": "sk-ant-(?:api[0-9]{2}-)?[A-Za-z0-9_-]{30,}",
        "match": "regex",
        "perm": "replace",
        "replace": "[REDACTED_SECRET_ANTHROPIC_API_KEY]",
        "reason": "Anthropic API Key redacted"
      },
      {
        "pattern": "(?:ghp|gho|ghu|ghs|ghr)_[A-Za-z0-9_]{36,}",
        "match": "regex",
        "perm": "replace",
        "replace": "[REDACTED_SECRET_GITHUB_TOKEN]",
        "reason": "GitHub Token redacted"
      },
      {
        "pattern": "github_pat_[A-Za-z0-9_]{80,}",
        "match": "regex",
        "perm": "replace",
        "replace": "[REDACTED_SECRET_GITHUB_PAT]",
        "reason": "GitHub Fine-Grained Token redacted"
      },
      {
        "pattern": "\\b(?:A3T[A-Z0-9]|AKIA|AGPA|AIDA|AROA|AIPA|ANPA|ANVA|ASIA)[A-Z0-9]{16}\\b",
        "match": "regex",
        "perm": "replace",
        "replace": "[REDACTED_SECRET_AWS_ACCESS_KEY]",
        "reason": "AWS Access Key redacted"
      },
      {
        "pattern": "(?i)\\b(aws_secret_access_key|aws_session_token)(\\s*[:=]\\s*)['\"]?([A-Za-z0-9/+=]{40})['\"]?",
        "match": "regex",
        "perm": "replace",
        "replace": "\\1\\2[REDACTED_SECRET_AWS_SECRET_KEY]",
        "reason": "AWS Secret Key redacted"
      },
      {
        "pattern": "AIza[0-9A-Za-z\\-_]{35}",
        "match": "regex",
        "perm": "replace",
        "replace": "[REDACTED_SECRET_GOOGLE_AI_KEY]",
        "reason": "Google AI / GCP Key redacted"
      },
      {
        "pattern": "xox[baprs]-[0-9]{10,13}-[0-9]{10,13}[a-zA-Z0-9-]*",
        "match": "regex",
        "perm": "replace",
        "replace": "[REDACTED_SECRET_SLACK_TOKEN]",
        "reason": "Slack Token redacted"
      },
      {
        "pattern": "(?i)\\b(bearer\\s+)[a-zA-Z0-9_\\-\\.]{25,}",
        "match": "regex",
        "perm": "replace",
        "replace": "\\1[REDACTED_BEARER_TOKEN]",
        "reason": "Bearer Token redacted"
      },
      {
        "pattern": "(?i)\\b(password|passwd|secret|api_key|apikey|access_token|auth_token)(\\s*[:=]\\s*)(?:['\"][^'\"]{8,}['\"]|[A-Za-z0-9!@#$%^&*()_+\\-=\\[\\]{};:,.<>?/]{8,})",
        "match": "regex",
        "perm": "replace",
        "replace": "\\1\\2[REDACTED_SECRET_PASSWORD]",
        "reason": "Password/secret assignment redacted"
      },
      {
        "pattern": "hf_[a-zA-Z0-9]{34,}",
        "match": "regex",
        "perm": "replace",
        "replace": "[REDACTED_SECRET_HUGGINGFACE_TOKEN]",
        "reason": "Hugging Face Token redacted"
      },
      {
        "pattern": "(?:sk|pk)_(?:live|test)_[0-9a-zA-Z]{24,}",
        "match": "regex",
        "perm": "replace",
        "replace": "[REDACTED_SECRET_STRIPE_KEY]",
        "reason": "Stripe API Key redacted"
      }
    ]
  }
}
