{
  "enableTelemetry": false,
  "model": "Gemini 3.5 Flash (Medium)",
  "permissions": {
    "allow": [
      "command(.agents/skills/github-review-orchestrator/scripts/fetch_all_pr_threads.sh)",
      "command(.agents/skills/github-review-orchestrator/scripts/fetch_file.sh)",
      "command(.agents/skills/github-review-orchestrator/scripts/get_pr_context.sh)",
      "command(.github/skills/github-review-orchestrator/scripts/fetch_all_pr_threads.sh)",
      "command(.github/skills/github-review-orchestrator/scripts/get_pr_context.sh)",
      "command(.github/skills/github-review-orchestrator/scripts/submit_review.sh)",
      "command(agy changelog)",
      "command(agy models)",
      "command(awk)",
      "command(cat)",
      "command(chezmoi)",
      "command(cut)",
      "command(date)",
      "command(diff)",
      "command(dirname)",
      "command(docker ps)",
      "command(du)",
      "command(gh issue view)",
      "command(gh label list)",
      "command(gh pr checks)",
      "command(gh pr diff)",
      "command(gh pr list)",
      "command(gh pr review)",
      "command(gh pr view)",
      "command(gh run list)",
      "command(gh run view)",
      "command(gh run watch)",
      "command(git add)",
      "command(git branch)",
      "command(git diff)",
      "command(git fetch)",
      "command(git grep)",
      "command(git log)",
      "command(git ls-files)",
      "command(git ls-tree)",
      "command(git merge-base)",
      "command(git rebase)",
      "command(git rev-list)",
      "command(git rev-parse)",
      "command(git rm)",
      "command(git show)",
      "command(git stash list)",
      "command(git stash show)",
      "command(git status)",
      "command(grep -r)",
      "command(grep)",
      "command(head)",
      "command(jq)",
      "command(ls)",
      "command(printf)",
      "command(ps)",
      "command(pwd)",
      "command(read)",
      "command(readlink)",
      "command(shellcheck)",
      "command(sleep)",
      "command(sort)",
      "command(stat)",
      "command(strings)",
      "command(tail)",
      "command(wc)",
      "command(which)"
    ]
  },
  "runningLightSpeed": "fast",
  "statusLine": {
    "type": "",
    "command": "",
    "enabled": true
  },
  "toolPermission": "proceed-in-sandbox",
  "trustedWorkspaces": [
    "~/src/dotfiles"
  ]
}